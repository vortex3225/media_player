<Window x:Class="Media_Player.SelectPlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Media_Player"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Title="Select a playlist to load" Height="138" Width="300" Icon="/Sprites/playlist_go.png">
    <Viewbox>
        <Grid Width="300" Height="138">
            <ComboBox x:Name="playlist_selector_box" Margin="0,18,0,84"/>
            <Button x:Name="confirm_btn" IsDefault="True" Content="Confirm" Background="LimeGreen" Click="confirm_btn_Click" FontFamily="Arial" FontSize="12" Margin="0,73,0,38"/>
            <Button x:Name="cancel_btn" IsCancel="True" Content="Cancel" Background="Red" Click="cancel_btn_Click" FontFamily="Arial" FontSize="12" Margin="0,104,0,10"/>
        </Grid>
    </Viewbox>
</Window>
